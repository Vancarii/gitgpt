{
  "version": 2,
  "builds": [
    { "src": "server/server.js", "use": "@vercel/node" },
    { "src": "web-build/**", "use": "@vercel/static" }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "server/server.js" }]
}
